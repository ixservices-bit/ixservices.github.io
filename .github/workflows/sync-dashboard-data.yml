name: Sync dashboard data

on:
  workflow_dispatch:
  schedule:
    - cron: "*/30 * * * *"

permissions:
  contents: write

env:
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true

jobs:
  sync:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Pages repo
        uses: actions/checkout@v4

      - name: Download private dashboard data
        env:
          DATA_TOKEN: ${{ secrets.ICQA_DATA_SYNC_TOKEN }}
          UPDATE_TOKEN: ${{ secrets.ICQA_UPDATE_SYNC_TOKEN }}
        run: |
          set -euo pipefail

          if [ -z "${DATA_TOKEN}" ]; then
            echo "Missing ICQA_DATA_SYNC_TOKEN repository secret."
            exit 1
          fi

          if [ -z "${UPDATE_TOKEN}" ]; then
            UPDATE_TOKEN="${DATA_TOKEN}"
          fi

          mkdir -p data

          download() {
            local repo="$1"
            local path="$2"
            local output="$3"
            local token="$4"
            local url="https://api.github.com/repos/ixservices-bit/${repo}/contents/${path}?ref=main"

            status=$(curl -L -sS \
              -H "Authorization: Bearer ${token}" \
              -H "Accept: application/vnd.github.raw" \
              -H "X-GitHub-Api-Version: 2022-11-28" \
              -o "${output}.tmp" \
              -w "%{http_code}" \
              "${url}")

            if [ "${status}" = "200" ]; then
              mv "${output}.tmp" "${output}"
              echo "Synced ${repo}/${path} -> ${output}"
            elif [ "${status}" = "404" ]; then
              rm -f "${output}.tmp"
              echo "Missing optional source ${repo}/${path}; keeping existing ${output}"
            else
              rm -f "${output}.tmp"
              echo "Failed ${repo}/${path} with HTTP ${status}"
              exit 1
            fi
          }

          download data FeatureUsage.csv data/FeatureUsage.csv "${DATA_TOKEN}"
          download data UserUsage.csv data/UserUsage.csv "${DATA_TOKEN}"
          download data usersettings/customization_usage.csv data/customization_usage.csv "${DATA_TOKEN}"
          download data quicklinks.csv data/quicklinks.csv "${DATA_TOKEN}"
          download data feedback.csv data/feedback.csv "${DATA_TOKEN}"
          download data managers.csv data/managers.csv "${DATA_TOKEN}"
          download data ActiveUsers.csv data/ActiveUsers.csv "${DATA_TOKEN}"
          download update icqa_version.txt data/icqa_version.txt "${UPDATE_TOKEN}"
          download update rdc_version.txt data/rdc_version.txt "${UPDATE_TOKEN}"

      - name: Commit synced data
        run: |
          set -euo pipefail
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add data

          if git diff --cached --quiet; then
            echo "No dashboard data changes."
            exit 0
          fi

          git commit -m "Sync dashboard CSV data"
          git push
